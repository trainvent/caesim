# Optional local config for the backend.
#
# The backend can read the Supabase connection string from the Linux Login keychain
# entry named `Ceasim_Supabase`, so this file does not need to contain secrets.
PORT=3000
